language: csharp
sudo: required

solution: UKPO2.sln

os: 
    - linux


mono:
    - 3.12.0
    - latest
    
install:
    - mono ./nuget.exe install TestStack.White -Version latest -OutputDirectory DNAGraphTests
script:
    - mkdir build
    - cd build
